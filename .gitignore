node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
Thumbs.db
.vscode/
.idea/

# Scratch space - notes and working files that are not part of the project
temp/

# Demo site, assembled by .github/workflows/pages.yml - never checked in, so the
# published demo can only ever be built from source
site/

# Board build artifacts - regenerate with: npm run build:board
test-boards/*/lib/
test-boards/*/ui.log

# Example board vendored library - copy from dist/ (see examples/persephone-board/README.md)
examples/persephone-board/lib/

# Local three-way comparison board (av-grid vs two third-party grids). Vendors ~2.5 MB of
# other people's bundles and is a working instrument, not a gate — see its own CLAUDE.md.
# Its av-grid build is dist/av-grid.umd.cjs, re-copied by hand after every source change.
test-boards/GridCompareBoard/
